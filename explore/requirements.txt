# iNaturalist API exploration dependencies
pyinaturalist>=0.19.0
python-dotenv>=1.0.0
requests>=2.31.0

# Credential storage in the OS vault (Windows Credential Manager / Keychain)
keyring>=25.0.0

# EXIF extraction for the local feasibility tests
pillow>=10.0.0

# Testing
pytest>=8.0.0
responses>=0.25.0
